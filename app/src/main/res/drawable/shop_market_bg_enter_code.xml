<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充颜色 -->
    <solid android:color="@color/white"></solid>

    <!-- 线的宽度，颜色 -->
    <!-- <stroke
         android:dashGap="0dp"
         android:dashWidth="1dp"
         android:width="1dp"
         android:color="@color/line_gary" >
     </stroke>-->
    <!-- 线的高度 -->
    <!-- <size android:height="6px" />  -->
    <stroke
        android:width="1dp"
        android:color="@color/line_gary" />
    <padding
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp" />
    <!-- <corners 
        android:radius="5dp"
        /> -->

</shape>